{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "description": "for fullstack module 3",
  "license": "MIT",
  "author": "Rod",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
      "start": "node index.js",
      "dev": "node --watch index.js",
      "test": "echo \"Error: no test specified\" && exit 1",
      "build:ui": "rm -rf dist && cd ../../part2/phonebook && npm run build && mv dist ../../3/phonebook-backend",
      "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "exrpess": "^0.0.1-security",
    "morgan": "^1.10.1"
  }
}
